# torch/torchaudio는 setup_env.py가 환경(home=CUDA / work=MPS)에 맞게 설치합니다.
# 여기엔 넣지 마세요. 나머지 의존성만 나열하세요.
transformers==4.46.3   # whisper-base + eager attentions. home 안전 핀과 동일(HF 캐시 공유). work에서 최신 필요시 조정.
datasets               # 예시 오디오(LibriSpeech dummy) 로드
soundfile              # datasets가 flac 디코딩에 사용
matplotlib             # 단계별 시각화 → outputs/*.png
