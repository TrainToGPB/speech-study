# torch/torchaudio는 setup_env.py가 환경(home=CUDA / work=MPS)에 맞게 설치합니다.
# 여기엔 넣지 마세요. 나머지 의존성만 나열하세요.
voxcpm==2.0.3          # 공식 추론 패키지. 기본은 VoxCPM2지만 run.py가 0.5B를 명시 로드.
transformers==4.46.3   # home(CUDA cu121+torch2.5) 안전 핀. work에서도 voxcpm과 그대로 호환됨(업그레이드 불필요).
